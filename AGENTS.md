# AGENTS.md

本仓库是《气数》的研究、编辑与内容单一真相源。

任何 AI / Agent 在进行实质性研究、改稿、结构调整、发布配置或分支操作前，应先阅读：

1. `README.md`
2. `METHODOLOGY.md`
3. `EDITORIAL.md`
4. 当前任务对应的 Issue / research / draft
5. 若任务涉及发布或环境流转，再确认本文的分支与发布规则

不要用旧聊天记忆、旧分支状态或模型印象代替仓库当前内容。

## 一、分支与环境

本项目采用固定流转：

```text
feature/* or research/* → dev → test → prod
                              │      │
                              │      └─ production / 正式发布
                              └─ preview / 预览与验收
```

### `dev`

日常开发、研究、写作与结构修改的集成分支。

- 所有新工作原则上从 `dev` 分出短期分支。
- 完成后通过 PR 合回 `dev`。
- `dev` 可以包含尚未正式发布的研究、文章、产品或流水线改动。
- 不因为“只是文档”就绕过研究与证据规则。

推荐分支命名：

- `research/<topic>`
- `essay/<slug>`
- `docs/<topic>`
- `chore/<topic>`
- `fix/<topic>`

### `test`

预览与人工验收分支，对应 preview 环境。

- 只接受来自 `dev` 的推进。
- 不在 `test` 上直接创作、研究或开发。
- 用于检查 tomz.io 预览、文章呈现、引用、结构、链接、渠道派生结果与发布前效果。
- 进入 `test` 不代表已经正式发布。

### `prod`

正式生产分支，对应公开发布状态。

- 只接受来自 `test` 的推进。
- 不直接提交日常修改。
- 进入 `prod` 的内容应已经完成必要的人审、证据审查与预览验收。
- `prod` 是“当前正式发布版本”的代码与内容真相源。

### `main`

`main` 为仓库创建阶段遗留分支，不再作为新的开发、预览或生产流转分支。

除非后续明确迁移用途，否则不要向 `main` 提交新工作，也不要以 `main` 判断当前项目状态。

### 推进与回灌

正常推进只能是：

`dev → test → prod`

禁止：

- `dev → prod` 跳级；
- feature 分支直接进入 `test` 或 `prod`；
- 在 `test` 或 `prod` 上直接长期开发；
- 用 force push 覆盖已经公开的生产历史，除非明确处理仓库事故。

若生产出现必须立即修复的问题：

1. 从 `prod` 创建短期 `fix/*`；
2. 验证修复；
3. 合入 `prod`；
4. 同一修复必须回灌 `dev`，必要时同步 `test`；
5. 不允许只修生产而让后续分支再次引入旧问题。

## 二、研究与写作工作流

标准流程：

```text
Seed
→ Dialogue
→ Question Contract
→ Research Pass 1
→ Dialogue 2
→ Adversarial Research
→ Draft
→ Evidence Review
→ test Preview
→ prod Publish
→ Revisit
```

除非只是错别字、失效链接或无实质含义的格式修复，不要跳过 Dialogue、Question Contract、Adversarial Research 与 Evidence Review。

### Issue 的角色

一篇重要文章或一个研究问题，优先使用 Issue 作为长期问题契约与讨论入口。

Question Contract 至少写清：

- 核心问题；
- 时间与空间范围；
- 需要解释的结果；
- 初始假设；
- 已知竞争解释；
- 什么证据可能推翻或显著修改初始判断。

不要把 Issue 写成“让 AI 写一篇文章”的一次性任务说明。

## 三、不可跳过的研究规则

- 不从作者既有观点预设结论。
- 重大历史与当代事实必须查来源，不凭模型记忆补全。
- 优先一手材料、同行评审研究、大学出版社与正式统计。
- 区分事实、解释、判断、推测。
- 因果文章必须主动寻找竞争解释和反证。
- 不把历史类比当成证明。
- 不因为某个材料符合当前叙事就降低证据标准。
- 引用必须真正支持相邻论述；不要“挂一个权威链接”冒充证据。
- 研究发现与初始假设冲突时，以证据为准，并保留观点变化记录。
- 对存在重要学术争议的问题，应写明争议与证据强弱，不制造虚假对称。

## 四、当代政治与国家研究

涉及当代国家、政治人物、政党、政策、选举或当前政治事实时：

- 写作时重新检索，不能依赖旧记忆；
- 记录 `evidence_as_of: YYYY-MM-DD`；
- 明确资料时点、统计口径、适用地区与人群；
- 国内外来源尽量交叉验证；
- 不从作者个人政治观点推导结论；
- 不对政治人物、政党或政策选择做总体排名、背书或推荐；
- 不自行预测选举胜负；
- 不用“气数”预测国家寿命、崩溃时间或制作伪精确分数；
- 将事实、来源观点、作者判断与推测保持可区分。

## 五、内容与目录边界

当前约定：

- `research/`：研究种子、研究卡、资料摘要、竞争解释、反证。
- `essays/`：经过研究形成的母稿；首次真正使用时再建立。
- `docs/`：项目、方法、产品、出版与协作说明。
- `cases/`、`mechanisms/`、`library/`：只有出现真实复用需求后再建立。

不要因为“以后可能需要”提前制造复杂 schema、目录或自动化。

同一事实不维护多份互相独立的正文真相。渠道版本应从同一研究母稿派生。

## 六、发布规则

GitHub 是研究与内容单一真相源，但不是普通读者的最终阅读界面。

`publication.json` 是《气数》的出版合同真相源：声明 Book 元数据、条目来源、slug、顺序、摘要、Tag 与署名状态。渠道仓库和渲染器只能读取并派生，不得维护第二份可编辑正文，也不得替《气数》推断未声明的作者关系。

目标渠道：

- tomz.io：完整阅读版与研究入口；
- 微信公众号：大众传播版；
- 未来书稿：独立再编辑版本。

### Preview

进入 `test` 后，优先验证：

- 正文是否完整；
- 链接与引用是否可用；
- 研究札记、争议与修订信息是否正确；
- 页面结构、标题、摘要和移动端阅读是否符合预期；
- 渠道派生是否改变了事实或关键判断；
- 实际 Preview 是否来自 `test` 的精确 commit SHA，而不是浮动分支或渠道仓库中的复制正文。

### Production

只有明确完成发布前检查后，才从 `test` 推进 `prod`。

公众号等具有外部副作用的正式发布：

- 可以自动生成草稿；
- 不应由 Agent 自动完成最终群发；
- 最终公开发布保留人工确认。

## 七、Agent 行为规范

Agent 应：

- 先读当前真实仓库状态，再做判断；
- 用小步、可审查、可回滚的修改推进；
- 优先修改最小必要范围；
- 在证据不足时明确说“不确定”；
- 主动指出会改变结论的反证；
- 研究、正文、渠道派生和发布配置尽量保持可追溯；
- 合并前检查目标分支是否正确。

Agent 不应：

- 为了流畅而补全未经验证的史实；
- 为迎合作者观点选择性搜证；
- 把“大家都知道”当作来源；
- 把通俗史、社交媒体或单条新闻独立承担重大因果结论；
- 把历史类比直接映射成当代政治结论；
- 在没有真实需求时搭建复杂工作流；
- 擅自跳过 `test` 直接进入 `prod`；
- 擅自执行最终公众号群发等不可逆外部动作。

## 八、完成标准

一个研究或内容任务只有在与其阶段相符的证据齐全时才算完成。

- 研究完成：核心事实、主要解释、竞争解释、证据薄弱处与开放问题已记录。
- Draft 完成：正文可读，关键事实可追溯，判断与不确定性表达匹配。
- Evidence Review 完成：引用、数据口径、反证、竞争解释和当代材料时效已复核。
- Preview 完成：`test` 环境中的实际呈现已检查，而不是只看源码或 CI。
- Publish 完成：对应版本已进入 `prod`，且需要人工确认的外部发布已经由人确认。

CI、Agent Review 或 AI Review 只能作为辅助证据，不能替代真实的研究审查、预览验收或人工发布确认。
